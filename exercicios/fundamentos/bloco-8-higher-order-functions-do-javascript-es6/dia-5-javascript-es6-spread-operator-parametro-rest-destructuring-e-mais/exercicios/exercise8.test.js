const {  } = require('./exercise8');

describe('Exercicio 8', () => { 
  test('Verifica se  é realmente uma função.', () => {
    expect(typeof toObject).toBe('function');
  });
  test(`Verifica se o retorno da função é `, () => {
    expect().toEqual();
  });
});